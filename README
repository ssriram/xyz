
___  ___ ___ __ ________
\  \/  /<   |  |\___   /
 >    <  \___  | /    / 
/__/\_ \ / ____|/_____ \
      \/ \/           \/ 

- a small scheme like implementation
    by ssriram
    http://thenaughtyguy.blogspot.com
  
- gave my own touch '~' implies not yet ready
    number type is double -0.234e-23.455
    hex integer 0x23 0xf4
    characters start with # and not #\
    escape sequences \r\n\t\s\\ a.k.a spl characters #\r #\n #\t #\s #\# #\\
    true ##t false ##f
    hex string "\x03\xde\x34"
    define is (def ...)
    functions a.k.a lambda forms are (fn ...)
    begin is (do ...)
    ~int mode number
    ~smart string
    ~garbage collection
    ~quasi-quote
    ~vector
    ~unquote
    ~unquote-splicing
    ~first class continuation
    ~call/cc
    not-ok object for error/invalid sexpr
    ~module system
    simple test case
    
- further plan
    ~network api
    ~write a shellcode generator
    ~write an exploit
    ~write an exploit generator
    ~bytecode compiler and interpreter/vm
